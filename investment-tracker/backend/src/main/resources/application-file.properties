# File Storage Development Profile
# Uses JSON file-based persistence (existing FileStorage implementation)

app.persistence.type=file

# Optional: configure file storage path if added later
app.storage.etf-file=etfs.json
